name: "Shellcheck"

on:
  push:
    branch:
      - main
  pull_request:

jobs:
  shellcheck:
    name: Shellcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.1.0
      - name: Run ShellCheck
        uses: ludeeus/action-shellcheck@master
